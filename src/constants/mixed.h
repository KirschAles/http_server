#ifndef HTTP_SERVER_MIXED_H
#define HTTP_SERVER_MIXED_H
#include <string>

namespace mixed {
    // Used as placeholder value for when the information couldn't be reached
    const std::string unknown = "Unknown.";
    const char divider = ',';
    const std::string default_config = "./config.txt";

}
#endif //HTTP_SERVER_MIXED_H
